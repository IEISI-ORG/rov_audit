# Ruff configuration for rov_audit
# E701/E722/F541 are pre-existing intentional compact style in rov_utils.py
# and statistics_v6.py — suppress them project-wide to keep CI green.
# F401/F841 (unused imports/variables) are always enforced.
[lint]
ignore = ["E701", "E722", "F541"]
