# API Keys and More
secrets.yaml

# Bin files and generated assets: No Please
*.pdf
*.png
*.html
*.pdf
*.gz
*.png

# BIG BGP FILE
latest-bview*
bgp-table.txt
table.txt

# Go Binaries
bgp-extractor
cone-calculator
fetch-roa

# Data and Outputs (Stop uploading all the data)
data/
output/
results/
reports/old/
reports/*.html

# Script Cruft
__pycache__/

# locks
.locks/

# rotated cron.log archives (see rov_cron.sh run_commit) — kept locally,
# never committed, to avoid recreating the unbounded-log-growth problem
# under a new filename
logs/cron.log.*

# Claude Code session state — local only, never commit
.claude/

